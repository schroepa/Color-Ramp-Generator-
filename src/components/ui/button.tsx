'use client'

/**
 * SmoothUI SmoothButton — Tintfield entry.
 * Liquid hover (~1.04 bouncy) / press (~0.97 snappy) / focus-visible / disabled.
 */
export {
  SmoothButton as Button,
  SmoothButton,
  smoothButtonVariants as buttonVariants,
  useLiquidMotion,
  type SmoothButtonProps as ButtonProps,
} from '@/components/ui/smoothui/smooth-button'
