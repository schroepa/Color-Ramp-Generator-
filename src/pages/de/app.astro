---
import App from '@/App'
import SiteHead from '@/components/seo/SiteHead.astro'
import '@/index.css'
import { withBase } from '@/i18n/copy'

const title = 'Tintfield · App'
const description =
  'Eine Farbe einfügen, eine kontrastsichere Farbskala für Tailwind, Radix, Material und mehr erhalten.'
const canonical = new URL(withBase('/de/app'), Astro.site).href
---

<!doctype html>
<html lang="de">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content={description} />
    <title>{title}</title>
    <link rel="canonical" href={canonical} />
    <link
      rel="alternate"
      hreflang="en"
      href={new URL(withBase('/app'), Astro.site).href}
    />
    <link rel="alternate" hreflang="de" href={canonical} />
    <SiteHead
      title={title}
      description={description}
      url={canonical}
      locale="de"
    />
  </head>
  <body class="min-h-svh bg-[var(--bg)] text-[var(--text)]">
    <App client:only="react" />
  </body>
</html>
